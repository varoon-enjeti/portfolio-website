import Body from "./body";

export default function Background() {
    return (
        <div className="h-screen w-screen main-background flex flex-col items-center justify-center">
            <Body />
        </div>
    );
}