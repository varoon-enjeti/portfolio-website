import React from 'react';
import "../../../styles/pages-styles/page-component-styles/projcard.css"

export default function ProjCard({name, description, tools, image, link}) {
    return (
        <div className="projcard"></div>
    );
}